const font = "'Gantari', sans-serif";

const typography: any = () => {
    return {
        fontFamily: font,
        color: "white",
        fontSize: 16,
        button: {
            fontStyle: 'italic',
        },
    };
};

export default typography;